spring:
  application:
    name: eurekaServer
  security:
    user:
      name: user
      password: user

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
